import React from 'react';

export default function Login(){
  return (
    <div>
        {/* Logo */}
        { /* Login form */}
        <h1>Login</h1>
        <button>Login</button>
    </div>
  );
};
